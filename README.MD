# Motion and Object Detection Project

## Описание проекта

Этот проект предназначен для анализа видеофайлов с камер видеонаблюдения с целью обнаружения движения и распознавания объектов (например, людей, машин, собак) с использованием модели YOLOv8. Проект обнаруживает движение в видео, применяет модель YOLOv8 для идентификации объектов из заданного списка классов, сохраняет кадры с обнаруженными объектами и записывает результаты в CSV-файл.

### Основные возможности
- **Обнаружение движения**: Анализирует видео на наличие движения с использованием разности кадров и контурного анализа.
- **Распознавание объектов**: Использует модель YOLOv8 (`yolov8s.pt`) для идентификации объектов из набора данных COCO (80 классов, таких как `person`, `car`, `dog` и др.).
- **Сохранение результатов**:
  - Сохраняет кадры с обнаруженными объектами в папку `motion_frames`.
  - Записывает информацию о времени обнаружения, классе объекта и уверенности в CSV-файл (`motions_log.csv`).
- **Параллельная обработка**: Поддерживает многопоточную обработку видео с использованием `ThreadPoolExecutor`.
- **Прогресс-бары**: Отображает прогресс обработки файлов и кадров с помощью библиотеки `tqdm`.
- **Гибкая настройка**: Позволяет выбирать классы объектов для распознавания, настраивать чувствительность движения и другие параметры.

Проект оптимизирован для работы на **Windows**, но может быть адаптирован для других ОС (Linux, macOS) с некоторыми изменениями.

## Требования

### Системные требования
- **Операционная система**: Windows (рекомендуется Windows 10 или выше).
- **Python**: Версия 3.7 или выше.
- **Аппаратное обеспечение**:
  - Минимум 8 ГБ оперативной памяти.
  - Процессор с 4+ ядрами для многопоточной обработки.
  - Для ускорения распознавания объектов рекомендуется GPU с поддержкой CUDA (опционально).

### Зависимости
Зависимости перечислены в файле `requirements.txt`:
- `opencv-python` – для обработки видео и изображений.
- `numpy` – для работы с массивами.
- `tqdm` – для отображения прогресс-баров.
- `ultralytics` – для работы с моделью YOLOv8.

### Модель YOLOv8
Проект использует предобученную модель `yolov8s.pt` (средняя по размеру и производительности). Вы можете заменить её на другие модели YOLOv8:
- `yolov8n.pt` – более лёгкая и быстрая, но менее точная.
- `yolov8m.pt`, `yolov8l.pt`, `yolov8x.pt` – более точные, но требуют больше ресурсов.

Файлы моделей (`yolov8s.pt`, `yolov8n.pt`) должны быть помещены в корневую директорию проекта.

## Установка

1. **Склонируйте или распакуйте проект**:
   Убедитесь, что у вас есть все файлы проекта, включая `motion_analysis_yolo.py`, `run_motion.cmd`, `requirements.txt`, и модели YOLOv8 (`yolov8s.pt`).

2. **Поместите видеофайлы**:
   Скопируйте видеофайлы (форматы `.mp4`, `.avi`, `.mov`, `.mkv`) в корневую директорию проекта (рядом с `motion_analysis_yolo.py`). Пример:
   ```
   Camera_25_ethernet-6_ethernet-6_20250801070002_20250801210002_519549.mp4
   Camera_25_ethernet-6_ethernet-6_20250802070027_20250802122814_552327.mp4
   ```

3. **Установите Python**:
   Убедитесь, что Python 3.7+ установлен. Вы можете скачать его с [официального сайта](https://www.python.org/downloads/).

4. **Запустите скрипт `run_motion.cmd`**:
   - Дважды щёлкните по файлу `run_motion.cmd` в проводнике Windows.
   - Скрипт автоматически:
     - Создаст виртуальное окружение (`venv`).
     - Установит зависимости из `requirements.txt`.
     - Запустит основной скрипт `motion_analysis_yolo.py`.

   **Примечание**: Если вы не хотите использовать `run_motion.cmd`, выполните следующие шаги вручную:
   ```bash
   python -m venv venv
   .\venv\Scripts\activate
   pip install --upgrade pip
   pip install -r requirements.txt
   python motion_analysis_yolo.py
   ```

## Использование

### Запуск проекта
1. Убедитесь, что видеофайлы находятся в корневой директории проекта.
2. Запустите `run_motion.cmd`:
   ```
   double-click run_motion.cmd
   ```
3. Проект начнёт обработку всех видеофайлов в текущей директории:
   - Прогресс будет отображаться в консоли с помощью прогресс-баров.
   - Логи записываются в консоль и файл `motion_debug.log`.
   - Результаты (время обнаружения, класс объекта, уверенность) сохраняются в `motions_log.csv`.
   - Кадры с обнаруженными объектами сохраняются в папку `motion_frames`.

### Пример вывода
```
2025-08-14 09:39:00 | INFO | 📦 Загружаю модель YOLOv8...
2025-08-14 09:39:02 | INFO | 🔍 Найдено 7 видеофайлов. Запускаю в 6 поток(ах)...
Обработка файлов:   0%|          | 0/7 [00:00<?, ?it/s]
2025-08-14 09:39:02 | INFO | ▶ Обработка файла: Camera_25_...10124266.mp4
Файл Camera_25_...10124266.mp4: 100%|██████████| 462604/462604 [00:10<00:00, 46260.4it/s]
2025-08-14 09:39:12 | INFO | 🧍 person (0.85) в Camera_25_...10124266.mp4, 10.25 сек
2025-08-14 09:39:12 | INFO | 💾 Сохранён кадр: motion_frames/Camera_25_...10124266_10.25_person.jpg
2025-08-14 09:39:12 | INFO | ✅ Завершена обработка Camera_25_...10124266.mp4
Обработка файлов:  14%|█▍        | 1/7 [00:10<01:00, 10.0s/it]
```

### Результаты
- **CSV-файл** (`motions_log.csv`):
  ```
  file,frame_time_sec,class,confidence
  Camera_25_...10124266.mp4,10.25,person,0.85
  Camera_25_...10135839.mp4,5.50,car,0.92
  ```
- **Кадры**:
  - Сохранены в папке `motion_frames` с именами вида `<имя_видео>_<время>_<класс>.jpg`.
- **Лог-файл** (`motion_debug.log`):
  - Содержит подробную информацию о процессе обработки, включая ошибки.

### Прерывание
- **В режиме `SHOW_WINDOW=True`**:
  - Нажмите `q` в окне видео, чтобы прервать обработку текущего файла.
- **В любой момент**:
  - Нажмите `Ctrl+C` в консоли, чтобы завершить выполнение. Промежуточные результаты сохраняются в `motions_log.csv`.

## Структура проекта
```
.
├── Camera_25_*.mp4        # Видеофайлы для обработки
├── motion_analysis_yolo.py # Основной скрипт анализа
├── motion_frames/         # Папка для сохранённых кадров
├── motions_log.csv        # Результаты обработки (CSV)
├── motion_debug.log       # Лог-файл
├── requirements.txt       # Зависимости проекта
├── run_motion.cmd         # Скрипт для запуска (Windows)
├── test video/            # Папка с тестовыми видео
│   └── Camera_25_*.mp4
├── venv/                  # Виртуальное окружение
├── yolov8n.pt             # Модель YOLOv8 (лёгкая, опционально)
├── yolov8s.pt             # Модель YOLOv8 (средняя, основная)
```

### Описание файлов
- **`motion_analysis_yolo.py`**: Основной скрипт, выполняющий анализ движения и распознавание объектов.
- **`run_motion.cmd`**: Скрипт для автоматической установки и запуска проекта на Windows.
- **`requirements.txt`**: Список зависимостей для установки через `pip`.
- **`motion_frames/`**: Папка, куда сохраняются кадры с обнаруженными объектами.
- **`motions_log.csv`**: CSV-файл с результатами (имя файла, время, класс объекта, уверенность).
- **`motion_debug.log`**: Лог-файл с информацией о процессе обработки.
- **`yolov8s.pt`**: Предобученная модель YOLOv8 (основная).
- **`yolov8n.pt`**: Лёгкая модель YOLOv8 (опционально).
- **`test video/`**: Папка с тестовыми видеофайлами.

## Настройка параметров
Параметры настраиваются в начале файла `motion_analysis_yolo.py`:
```python
SENSITIVITY = 25              # Чувствительность обнаружения движения
MIN_AREA = 800                # Минимальная площадь движения (пиксели)
SHOW_WINDOW = False           # Показывать видео (True/False)
WINDOW_SCALE = 0.6            # Масштаб окна видео
PLAYBACK_SPEED = 16           # Скорость воспроизведения
SAVE_FRAMES = True            # Сохранять кадры
SAVE_DELAY_SEC = 2            # Задержка между сохранением кадров (сек)
RECOGNITION_DELAY_SEC = 3     # Задержка между распознаваниями YOLO (сек)
OUTPUT_FILE = "motions_log.csv"  # Имя CSV-файла
FRAMES_DIR = "motion_frames"  # Папка для кадров
MAX_THREADS = 6               # Количество потоков
YOLO_MODEL = "yolov8s.pt"     # Модель YOLOv8
CONF_THRESHOLD = 0.7          # Порог уверенности для YOLO
TARGET_CLASSES = ["person", "car", "dog"]  # Классы для распознавания
```

### Распознаваемые объекты
Модель `yolov8s.pt` поддерживает **80 классов объектов** из датасета COCO, включая:
- `person` (люди)
- `car` (машины)
- `dog` (собаки)
- `bicycle`, `motorcycle`, `bus`, `truck`, `cat`, `horse`, и т.д. (полный список см. в документа